<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.ex01.mapper.OrderMapper">
    
    <insert id="insert">
    	<![CDATA[
			insert into orderitem(orderId, id, postCode, address1, address2, address3, orderPrice, charge) values
			values(#{orderId, jdbcType = VARCHAR}, #{id}, #{postCode}, #{address1}, #{address2}, #{address3},
			#{orderPrice}, #{charge})
        ]]>
    </insert>
    
    <insert id="orderItemInsert">
		<selectKey keyProperty="oino" order="BEFORE" resultType="Long">
			select nvl(max(oino), 0) + 1 from orderitem
		</selectKey>
		
		<![CDATA[
			insert into orderitem(oino, orderId, ino, id, price, amount) values
        ]]>
		
        <foreach collection="list" item="item" index="index" open="(" separator="," close=")">
			(#{oino}, #{item.orderId}, #{item.ino}, #{item.price}, #{item.amount})
		</foreach>
	</insert>
 
</mapper>